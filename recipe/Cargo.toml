[package]
name = "recipe"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
traffic = { path = "../traffic" }
sum_slice = { path = "../sum_slice" }
output_area = { path = "../output_area" }
